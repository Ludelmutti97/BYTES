function stringSuperReduzida(str) {
    // Escreve aqui o teu código
 
  let arr = str.split("")

for(let i =0; i< arr.length; i++){
    if(arr[i] === arr[i+1]){
        arr.splice(i,2)
        i=-1
    }
}

    return arr.length ===0 ? "String Vazia" : arr.join("")

       
}
