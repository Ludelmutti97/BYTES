function valorMaximoImpar(array){
    // escreve aqui a função
    return array.reduce((acc,cur) => {
        if(cur % 2 !== 0 && cur > acc){
            return cur
        }
        return acc
    }) 
}
