// 1-Cria uma função que mapeia cada elemento de um array de strings para o seu comprimento.

const teste = ["afdg", "bb", "ccc", "ddhjkhkd"]

function mapeamentoDeArrayParaString(array) {
    return (array.map((element,i) => element.length))
}

console.log(mapeamentoDeArrayParaString(teste))

//2-Cria uma função que filtra os elementos pares, mapeia cada elemento para o seu quadrado e por fim reduz o array para calcular o somatório dos elementos resultantes.


function calculaSomatorioDoQuadradosDosPares(array) {




}



//3-Cria uma função que mapeia cada elemento de um array de strings para a string invertida.
//A inversão de cada string deve ser implementada utilizando uma função recursiva.



//4-Cria uma função que retorna true se todos os elementos do array forem pares.



//5-Cria uma função que retorna true se pelo menos um dos elementos do array for par.



//6-Cria uma função que reduz um array de números para um Map que tem como chaves os valores do array e como valores o número de ocorrências da chave. (mapa de ocOrrências)




//Cria uma função que mapeia cada elemento de um array de objetos com as propriedades name e birthday para um objeto que além destas propriedades, tem a propriedade age.